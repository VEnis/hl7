subprojects {
    apply plugin: 'groovy'

    dependencies {
        testCompile libs.groovy
        testCompile(libs.spockCore) {
            exclude group: 'org.codehaus.groovy', module: 'groovy-all'
        }
        testCompile(libs.spockReports) { transitive = false }
    }
}